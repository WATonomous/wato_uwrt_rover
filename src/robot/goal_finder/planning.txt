Things still gotta add:
-need to subscribe to a topic that can give information about what object its trying to detect, should then also switch the robot state into searching
-also need a way to switch state back to waiting for object when robot has moved to the goal_point, somehow connect to control node
-way to do this is make a function that checks distance to goal point, if its close enough switches state back to waiting, add this function
into if else block in timer callback

-currently some error with the yolo inference messages
-also gotta update docker files?
